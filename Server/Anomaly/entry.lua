
function config_entry(Entry)
end
function after_create_entry(Entry)
end
function after_destroy_entry_Anomaly(Entry)
end
function after_player_login_Anomaly(Entry, Player)
end
function check_can_enter_Anomaly(Player, CopyMGR)
end
function begin_enter_Anomaly(Player, CopyMGR)
end
